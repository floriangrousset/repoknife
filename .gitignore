# Claude Code plugin state (remember plugin logs)
.remember/

# local Claude settings
.claude/settings.local.json

# macOS
.DS_Store

# build artifacts (make build)
dist/

# staging for the separate floriangrousset/homebrew-tap repo (not part of this repo)
dist-tap/
